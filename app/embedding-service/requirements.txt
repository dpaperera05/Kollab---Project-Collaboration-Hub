# Core web framework
fastapi>=0.111.0

# ASGI server — used to run the FastAPI app
uvicorn[standard]>=0.29.0

# Sentence embedding model
sentence-transformers>=2.7.0

# Data validation (FastAPI uses this internally; listed explicitly for clarity)
pydantic>=2.7.0
